@inherits FieldCreatorBase<T>
@typeparam T

<MudExpansionPanels MultiExpansion="true">
    @foreach (var config in FilterableConfigs)
    {
        <MudExpansionPanel Text="@config.DisplayName">
            @CreateFilterField(config)
        </MudExpansionPanel>
    }
</MudExpansionPanels>